name: Test Runner

on:
  push:
    branches:
      - main

jobs:
  test-runner:
    runs-on: [self-hosted, Linux, X64]   # 👈 Dùng đúng 3 label của Kkanger
    steps:
      - name: Print runner info
        run: |
          echo "Runner is running!"
          echo "Hostname: $(hostname)"
          echo "Current dir: $(pwd)"
          echo "Files:"
          ls -al
