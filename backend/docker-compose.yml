# version: '3.8'  # Deprecated in Docker Compose v2

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: file-sharing-db
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-file_sharing_db}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=en_US.UTF-8"
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./pkg/database/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
      - ./pkg/database/demo_queries.sql:/docker-entrypoint-initdb.d/02-demo.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-network
    restart: unless-stopped
    profiles:
      - local-db

  # Backend Application (Development)
  app-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: file-sharing-app-dev
    env_file:
      - .env
    environment:
      - APP_ENV=development
      - APP_PORT=8080
      - DB_HOST=${DB_HOST:-postgres}
      - DB_PORT=${DB_PORT:-5432}
      - DB_USER=${DB_USER:-postgres}
      - DB_PASSWORD=${DB_PASSWORD:-postgres}
      - DB_NAME=${DB_NAME:-file_sharing_db}
      - DB_SSLMODE=${DB_SSLMODE:-disable}
      - RUN_DB_MIGRATIONS=${RUN_DB_MIGRATIONS:-true}
      - MIGRATIONS_PATH=${MIGRATIONS_PATH:-migrations}
      - JWT_SECRET=${JWT_SECRET:-dev_secret_key_minimum_32_characters_long}
      - STORAGE_PATH=/app/storage/uploads
      - CLOUD_STORAGE_ENABLED=${CLOUD_STORAGE_ENABLED:-true}
      - CLOUD_STORAGE_PROVIDER=${CLOUD_STORAGE_PROVIDER:-azure}
      - CLOUD_STORAGE_ENDPOINT=${CLOUD_STORAGE_ENDPOINT:-https://dathhcmut20251.blob.core.windows.net}
      - CLOUD_STORAGE_ACCESS_KEY=${CLOUD_STORAGE_ACCESS_KEY:-dathhcmut20251}
      - CLOUD_STORAGE_SECRET_KEY=${CLOUD_STORAGE_SECRET_KEY}
      - CLOUD_STORAGE_PUBLIC_CONTAINER=${CLOUD_STORAGE_PUBLIC_CONTAINER:-public-file}
      - CLOUD_STORAGE_PRIVATE_CONTAINER=${CLOUD_STORAGE_PRIVATE_CONTAINER:-private-file}
    ports:
      - "8082:8080"
    volumes:
      - .:/app
      - app_storage:/app/storage/uploads
      - go_modules:/go/pkg/mod
    networks:
      - app-network
    restart: unless-stopped
    profiles:
      - dev

  # Backend Application (Production)
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: file-sharing-app
    env_file:
      - .env
    environment:
      - APP_ENV=production
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - RUN_DB_MIGRATIONS=${RUN_DB_MIGRATIONS:-true}
      - MIGRATIONS_PATH=${MIGRATIONS_PATH:-migrations}
      - JWT_SECRET=${JWT_SECRET}
      - CLOUD_STORAGE_ENABLED=true
      - CLOUD_STORAGE_PROVIDER=azure
      - CLOUD_STORAGE_ENDPOINT=${CLOUD_STORAGE_ENDPOINT}
      - CLOUD_STORAGE_ACCESS_KEY=${CLOUD_STORAGE_ACCESS_KEY}
      - CLOUD_STORAGE_SECRET_KEY=${CLOUD_STORAGE_SECRET_KEY}
      - CLOUD_STORAGE_PUBLIC_CONTAINER=${CLOUD_STORAGE_PUBLIC_CONTAINER:-public-file}
      - CLOUD_STORAGE_PRIVATE_CONTAINER=${CLOUD_STORAGE_PRIVATE_CONTAINER:-private-file}
    ports:
      - "${APP_PORT:-8080}:8080"
    volumes:
      - app_storage:/app/storage/uploads
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    profiles:
      - prod

  # Database Migrations
  migrate:
    image: migrate/migrate:latest
    container_name: file-sharing-migrate
    volumes:
      - ./migrations:/migrations
    env_file:
      - .env
    networks:
      - app-network
    entrypoint: ["/bin/sh", "-c"]
    command: >
      migrate
      -path /migrations
      -database "postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=${DB_SSLMODE:-disable}"
      up
    profiles:
      - tools

  # Adminer (Database management UI)
  adminer:
    image: adminer:latest
    container_name: file-sharing-adminer
    ports:
      - "8081:8080"
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    depends_on:
      - postgres
    networks:
      - app-network
    restart: unless-stopped
    profiles:
      - tools

  # Nginx (Reverse Proxy)
  nginx:
    image: nginx:alpine
    container_name: file-sharing-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - app
    networks:
      - app-network
    restart: unless-stopped
    profiles:
      - prod

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  app_storage:
    driver: local
  go_modules:
    driver: local

networks:
  app-network:
    driver: bridge
